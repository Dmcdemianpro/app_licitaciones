# Copia este archivo a .env y ajusta los valores para tu entorno
# Formato correcto para SQL Server con Prisma (user/password como parámetros, no en el userinfo)

# Base de Datos SQL Server
DATABASE_URL="sqlserver://YOUR_SERVER:1433;database=YOUR_DATABASE_NAME;user=YOUR_USER;password=YOUR_PASSWORD;encrypt=true;trustServerCertificate=true"

# NextAuth - Genera una clave secreta con: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
NEXTAUTH_SECRET="your-secret-key-min-32-characters-here"
# IMPORTANTE: Para desarrollo local usa localhost, para producción usa la IP del servidor
# Ejemplos:
#   Desarrollo: http://localhost:3001
#   Producción: http://10.7.50.130:3001 (usar la IP real del servidor)
NEXTAUTH_URL="http://localhost:3001"
AUTH_TRUST_HOST="true"

# Entorno
NODE_ENV="development"

# Versión de la aplicación (incrementar este número al desplegar para invalidar sesiones antiguas)
# Formato: YYYYMMDD-HHMM o cualquier identificador único
# Ejemplo: 20260113-1500
APP_VERSION="1.0.0"

# API de Mercado Público
# Solicita este token a la unidad de desarrollo
MERCADOPUBLICO_APIKEY="YOUR-MERCADOPUBLICO-UUID-TOKEN-HERE"
